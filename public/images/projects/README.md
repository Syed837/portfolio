Drop real project screenshots here (e.g. `ztna-simulator.png`) and set the
matching project's `coverImage` field in `data/projects.ts` to
`/images/projects/your-file.png`. Until then, each project card/detail page
renders generated cover art based on its category — see
`components/projects/project-cover.tsx`.
